# omni.flux.stage_manager.plugin.interaction.usd

Provides reusable USD interaction plugins for the Stage Manager. Hidden filters (`display=False`) can still constrain
Stage Manager results, but their UI placement state is ignored when deciding whether filtered results should be expanded.
