# lightspeed.trex.camera_properties.shared.widget

Shows editable camera properties.

When the selected camera is the capture game camera, the widget copies the current view to `/OmniverseKit_Persp` and
edits the perspective camera instead. Capture camera values remain owned by the loaded capture.
