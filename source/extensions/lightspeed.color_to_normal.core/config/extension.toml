[package]
# Semantic Versionning is used: https://semver.org/
version = "0.1.0"

# Lists people or organizations that are considered the "authors" of the package.
authors = ["Alexander Jaus <ajaus@nvidia.com>"]

# The title and description fields are primarly for displaying extension info in UI
title = "Color Texture To Normal Map Converter"
description="Invokes an AI upscaler on textures passed to it."

# Path (relative to the root) or content of readme markdown file for UI.
readme  = "docs/README.md"

# URL of the extension source repository.
repository="https://gitlab-master.nvidia.com/lightspeedrtx/lightspeed-kit/-/tree/master/source/extensions/lightspeed.color_to_normal.core"

# One of categories for UI.
category = "Export"

# Keywords for the extension
keywords = ["convert", "normal", "color"]

# Location of change log file in target (final) folder of extension, relative to the root.
# More info on writing changelog: https://keepachangelog.com/en/1.0.0/
changelog="docs/CHANGELOG.md"

[dependencies]
"lightspeed.common" = {}

[python.pipapi]
requirements = ["Pillow", 
    "torch>=1.4.0",
    "torchvision>=0.5.0",
    "dominate>=2.4.0",
    "wandb"
]
use_online_index = true

# Main python module this extension provides, it will be publicly available as "import omni.example.hello".
[[python.module]]
name = "lightspeed.color_to_normal.core"
