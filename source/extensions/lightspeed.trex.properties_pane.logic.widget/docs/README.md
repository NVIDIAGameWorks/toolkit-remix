# Overview

This is the widget that creates a properties panel for Remix Logic OmniGraph prim types.

Embedding panes can call `expand_all_groups()` and `collapse_all_groups()` to bulk-toggle grouped logic property rows.
