.defaults:
  only:
    - merge_requests
  dependencies: []
  variables:
    GIT_STRATEGY: fetch

stages:
  - check_format
  - check_lint
  - build
  - package_and_publish

include:
  - local: tools/ci/gitlab/check_format.yml
  - local: tools/ci/gitlab/check_lint.yml
  - local: tools/ci/gitlab/build.yml
  - local: tools/ci/gitlab/package_and_publish.yml
