description = """
PROACTIVELY use this agent for omni.ui work — widgets, layouts, styles, MVD patterns, event handling.

Examples:
- "Build a property panel" → dispatch ui-expert
- "Fix the tree view not updating" → dispatch ui-expert
- "Add a search filter widget" → dispatch ui-expert"""
developer_instructions = "@.agents/subagents/ui-expert.md"
name = "ui-expert"
