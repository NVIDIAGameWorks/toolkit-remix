check-code-lint:
  extends: .defaults
  stage: check_lint
  needs:
      - check-code-format
  script:
    - .\lint_code.bat
  except:
    variables:
      - $CI_MERGE_REQUEST_TITLE =~ /\[pipeline:lint:skip\]/
  timeout: 10m
