.unit_tests_linux:
  extends:
    - .defaults
    - .linux_gpu_agent
    - .linux_vault
    - .soak_test_rules
  needs: []
  before_script:
    - !reference [ .linux_vault, before_script ]
    - !reference [ .refresh_usd_asset_checkout_linux, before_script ]
    - ./build.sh --rebuild --release
  rules:
    - !reference [.defaults, rules]
    - !reference [.soak_test_rules, rules]
