.e2e_tests_windows:
  extends:
    - .defaults
    - .windows_vault
    - .windows_agent_gpu
  needs:
    - check-lint
  before_script:
    - !reference [ .windows_vault, before_script ]
    - .\build.bat --rebuild --release
    - .\repo.bat publish_exts --verify
