check-test-location:
  stage: check_mr
  extends:
    - .defaults
    - .windows_build_agent
    - .soak_check_rules
  needs: []
  script:
    - .\repo.bat check_test_file_location
  rules:
    - !reference [.defaults, rules]
    - !reference [.soak_check_rules, rules]
