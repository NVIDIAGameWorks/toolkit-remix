check-lint:
  stage: checks_final
  extends:
    - .defaults
    - .windows_build_agent
    - .soak_check_rules
  needs: []
  script:
    - .\lint_code.bat all
  timeout: 10m
  rules:
    - !reference [.defaults, rules]
    - !reference [.soak_check_rules, rules]
