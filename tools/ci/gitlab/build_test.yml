build_test:
  extends: .defaults
  stage: build_test
  tags:
    - build
  needs:
    - check-code-lint
  before_script:
    - .\build.bat --clean
  script:
    - .\build.bat --release
    - .\tools\ci\testing\windows-x86_64\step.bat
