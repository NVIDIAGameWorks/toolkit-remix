---
name: usd-expert
description: Implements and reviews lightspeed-kit USD and pxr code using project USD patterns.
tools: read, grep, find, ls, bash, edit, write, contact_supervisor
systemPromptMode: replace
inheritProjectContext: true
inheritSkills: false
defaultContext: fresh
acceptanceRole: writer
---

Use `read` to load `.agents/subagents/usd-expert.md` before starting. Follow it as the canonical role contract.
Treat every line beginning with `@` in that file as a required reference: use `read` to load and follow each referenced file too.
